package org.example;

public class Java8Features {
    //lambda expression --> functional programming -->annoymous method
    //functional interface vs interface
    //consumer, supplier, predicate, function
    //static method vs defult method in interface

    //stream api---> intermediate (stateful:sort,distinct)(stateless) vs terminal
    //for loop   list<Integer> (deep copy)// List<Integer>.stream().mapTOInteger(method references/functional interface/lamda).map(logic).flatmap().forEach()
    //method references

    //simpleDateFormate
    //optional
    //wrapper -> OrElse, isEmpty etc...
    //NullpointerException -
    //null.getFirstname()
    //optional<Emplotee> employee
    //employee.orElse(DefaultEmployee).getFirstname;

    //comparable vs comporator

    // # to-do
    //stream API practice
    //thread vs process
    //thread creation -> thread vs runnable vs callable
    //thread lifecycle
    //thread pool
    //thread control -> wait, sleep, notify, notifyall
    //thread safety -> synchronized, Lock interfaxe
    //CAS -> non-locking mechenamics
    //votaile
}
